# Local development (SQLite - sem instalação)
DATABASE_URL="file:./dev.db"

# Produção (Railway - PostgreSQL fornecido automaticamente)
# DATABASE_URL="postgresql://..."  ← Railway preenche automaticamente

JWT_SECRET="troque-por-uma-chave-secreta-longa-e-aleatoria"
PORT=3001
NODE_ENV=development

# URL do frontend (para CORS em produção)
# FRONTEND_URL="https://seu-frontend.railway.app"
